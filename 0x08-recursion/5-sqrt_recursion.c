#include "main.h"
/**
 * _sqrt_recursion - function that give the square of a num
 * @n: int given 
 * Return: -1 in case of num  is odd else gives the square root.
 */
int _sqrt_recursion(int n)
{
	int res;

	if (n % 2 != 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
	}
}
