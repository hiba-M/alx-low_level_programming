#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'from a to z lower case'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (char i = 97; i < 123; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
