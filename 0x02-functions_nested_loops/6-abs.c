#include "main.h"
/**
 * _abs - the absolute value of n
 * @n: number given
 * Return: n if positive else -n if negative
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
